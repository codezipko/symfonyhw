{% extends 'base.html.twig' %}

{% block title %}Orai!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if app.request.query.get('day') is not empty %}
        Parametras {{ app.request.query.get('day') }}
    {% endif %}
     <ul>
         <li>Siandien {{ weatherData.date }}</li>
         <li>Temp diena {{ weatherData.dayTemp }}</li>
         <li>Temp nakti {{ weatherData.nightTemp }}</li>
         <li>Dangus {{ weatherData.sky }}</li>
         <li>Provider {{ weatherData.provider }}</li>
     </ul>
</div>
{% endblock %}
